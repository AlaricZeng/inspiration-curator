# LLM Provider — choose "openai" or "anthropic"
LLM_PROVIDER=openai

# API Keys — only the one matching LLM_PROVIDER is required
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# Daily scrape time (24h HH:MM, local time)
SCRAPE_TIME=08:00
